import { default as AccountEntity } from './account.entity'
import { default as InvoiceEntity } from './invoice.entity'

const Entities = [AccountEntity, InvoiceEntity]

export { AccountEntity, InvoiceEntity }

export default Entities
